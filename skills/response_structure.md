# Response Structure

- Always specify the file path before showing code changes.
- Format: `**File:** src/components/Example.tsx` followed by the code block.
- If multiple files are changed, group them with headers.
- Use `diff` or `patch` format for complex changes to show exactly what was added/removed.
- Never use markdown tables for technical explanations — use lists or plain text.